# 闪电链性能优化方案

## 目标

支持大量投射物时保持当前闪电链的时长、回闪、随机形状和命中特效；路径视觉改用轻量像素采样绘制，避免沿路径创建大量粒子。

## 问题

每支箭都会独立触发完整连锁；连锁内部又按投射物数生成电弧，并以约每 3.5 像素一次的频率创建粒子和光源。高投射物数量会快速触发大量重复事件。分裂箭会进一步放大这条链路。

## 方案

1. 每条闪电链由轻量 `Node2D` 保存 24 像素间隔的随机控制点，并以约 3.25 像素间隔在控制点段内用 `_draw()` 绘制带微抖动、随机旋转的小型白色像素矩形；这些采样不会创建独立粒子对象。
2. 每个脉冲绘制白色小型像素电弧，粒子随机旋转并保留低透明度白色光晕；不绘制连续几何线，主脉冲和回闪使用独立随机路径。
3. 每条脉冲只保留一个聚合光源，不再为每个采样段创建光源；命中点的闪光和火花仍由 `ParticleWorld` 负责。
4. 伤害、麻痹和连锁目标选择仍按每支箭结算；视觉路径不参与碰撞和玩法逻辑。

分裂箭的子箭完全不创建拖尾；只有普通主箭使用原有 `projectile_trail` 预设。

爆炸附魔统一使用 `explosion_burst`：所有粒子尺寸固定为 `4x4`，单次基础数量提高到 `96`，从爆炸中心出生并向四周发散，重力和阻力均为零，地形破坏只会增加一次同中心爆发。

电火花附魔在投射物命中点创建一个轻量效果节点，预警 `0.5` 秒期间只绘制旋转的黄色虚线圆圈；随后复用闪电链的方块采样路径，从 `260px` 高度落下闪电并对圆圈范围内敌人造成一次伤害。

## 约束

- 不降低闪电链持续时间、回闪次数、随机幅度或命中冲击火花。
- 不通过提高全局粒子上限解决问题。
- 高投射物时路径只保留有限控制点、方块采样点和脉冲记录；命中核心粒子和爆炸粒子仍由 `ParticleWorld` 的全局预算控制。

## 验收

十支以上箭矢同时命中时，闪电形状与当前一致，命中伤害和麻痹次数不变；路径采样只绘制小方块，不创建独立粒子对象或逐采样点光源。
