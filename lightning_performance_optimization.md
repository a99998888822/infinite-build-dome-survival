# 闪电链性能优化方案

## 目标

支持大量投射物时保持当前闪电链的时长、回闪、随机形状和命中特效，不以减少视觉效果换取性能。

## 问题

每支箭都会独立触发完整连锁；连锁内部又按投射物数生成电弧，并以约每 3.5 像素一次的频率创建粒子和光源。高投射物数量会快速触发大量重复事件。

## 方案

1. 每次攻击创建一个 `attack_id`，所有箭矢将闪电线段提交给同一个批量渲染器。
2. 批量渲染器保留每条线段、主脉冲、回闪和随机路径，但一次性绘制，避免逐采样点创建粒子事件。
3. 每条链脉冲只保留起点和终点的聚合光源，不再为每个线段创建光源。
4. 伤害、麻痹和连锁目标选择仍按每支箭结算；同一次攻击共享敌人查询结果。

## 约束

- 不降低闪电链持续时间、回闪次数、随机幅度或命中冲击火花。
- 不通过提高全局粒子上限解决问题。
- 高投射物时优先批量提交；超出预算时延后绘制，不丢弃已结算的效果。

## 验收

十支以上箭矢同时命中时，闪电形状与当前一致，命中伤害和麻痹次数不变，且不再出现明显卡顿。
