# 电火花工具输入

两份 WAV 是电火花方案处理工具仍使用的稳定输入：单次源声和带间隔的试听排列。原审阅网页、其他方案和旧决策已清理。

当前正式音效使用上级目录的 `lightning_bright_approved.wav`；这里的文件仅供 `build_electric_spark_c_refinement.py` 和 `build_electric_spark_c_directions.py` 复现处理。
